export type Followers = Follower[];

export type Follower = {
  diff_previous_price: number;
  first_name: string;
  id: number;
  last_name: string;
  near_match_date: string;
  rank: number;
  sport_id: number;
  team_id: number;
};
